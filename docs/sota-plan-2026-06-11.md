# Terax SOTA Plan (2026-06-11)

Supersedes `docs/architecture-review-2026-06.md` and `docs/improvement-plan-2026-06.md` (both written 2026-06-10, one day and six commits earlier). This document re-audits the codebase after the webview-native Pi agent landed, states the target architecture for the best version of this idea, and lays out the execution plan. Three parallel deep audits (pi runtime, workflow, Rust backend delta) plus direct verification back every claim.

---

## 1. The idea, stated precisely

Terax is an ultra-lightweight, AI-native terminal emulator. The product thesis that everything else must serve:

- **Terminal first.** xterm + PTY is the primary surface; editor, explorer, git are supporting cast.
- **Agent as a primitive, not a bolt-on.** A local agent runtime with sessions, fork/rollback, tool use, skills, and project memory (TERAX.md), BYOK across cloud and local providers.
- **Trust by default.** Every OS-touching operation crosses one audited Rust boundary; secrets live in the OS keychain; no telemetry.
- **Featherweight.** ~7-8 MB app, no bundled runtimes, unused features cost zero.

The SOTA version of this idea is the codebase where those four statements are true without exceptions. Today each has exactly one major exception, listed in section 3.

---

## 2. Where the codebase stands (delta since the 2026-06-10 review)

### 2.1 What shipped (commits 8560add..3d4fcff, +22.5k lines, 220 files)

**The webview-native Pi agent reached sidecar parity and became the default** (`USE_WEBVIEW_AGENT = true`, `pi-session.ts:545`). This is the decisive architectural event. The runtime now works like this:

- The agent loop runs in the webview on `@earendil-works/pi-agent-core` (`Agent`), with `pi-ai` streaming. The older SOTA doc's `createAgentSession` plan was abandoned in favor of bringing the hand-rolled path to parity. That pivot is accepted (see D1).
- Tools (`read_file`, `list_directory`, `grep`, `glob`, `write_file`, `edit_file`, `bash_run`, `ask_question`, plus discovered MCP tools) execute through standard Tauri commands via `pi-tools.ts`.
- Approval policies come from Rust (`pi_capability_manifest`, manifest is the single authority) but are enforced by an `approvalGate` wrapper in the webview (`pi-session.ts:425-449`), with a 5-minute auto-deny timeout.
- All LLM traffic is proxied through the SSRF-guarded Rust client (`ai_http_stream` / `ai_http_request`) via a ref-counted global fetch override (`pi-http.ts`).
- Persistence is two-file via new Rust commands: `pi_store_record_session/events` (capped 500-event log in `pi-sessions.json`) and `pi_store_record/load/delete_transcript` (canonical `AgentMessage[]`, atomic temp-file-rename writes, FNV-1a-hashed filenames).
- Fork, rollback, archive/restore, interactive questions, model switching on resume: all working in the webview path.

**Also landed:** capability framework applied to `fs_write_file`, `fs_delete`, `shell_run_command`, workflow file/http operations; MCP client refactor (connections split, OAuth rewrite, config store); 7 Rust integration test files (~4k LOC); idle-shutdown panic fix (`tokio::spawn` to `tauri::async_runtime::spawn`, with regression test); workflow gained schedule/webhook triggers, subgraphs, version snapshots, validation/docs panels; sidecar bundle shrunk; app shell split started (`AppSidebars.tsx`, `AppWorkspaceSurface.tsx`).

**Uncommitted:** a voice/TTS module (`src-tauri/src/modules/voice/`, Cartesia API + macOS AVSpeech via rodio, gated behind the `openclicky` feature) and a read-aloud action in `PiTranscript.tsx`.

### 2.2 Findings from yesterday now resolved

| 2026-06-10 finding | Status |
|---|---|
| Missing `pi_native_tool` blocked migration | Obsolete: tools route through existing audited-surface commands instead |
| Tool-name mapping mismatch | Fixed: explicit `MANIFEST_TO_WEBVIEW_TOOL` map in `tool-approval-policy.ts:48-56` |
| Two overlapping approval gates | Fixed: one gate (webview), one policy source (Rust manifest) |
| Provider config and model catalog triplicated | Largely fixed: webview path consumes `ai/config.ts`; only sidecar JS copies remain |
| Sidecar approval state lost on crash | Moot in the default path (webview registry + persisted events) |
| Vestigial `skills.rs` | Deleted |
| Idle-shutdown crash risk | Fixed and regression-tested |

### 2.3 What the pivot regressed, and what remains open

**The new #1 issue: approval enforcement moved to the wrong side of the trust boundary.** In the sidecar design, Rust verified every tool execution (`execute_verified_native_tool_with_policy`). In the webview design, Rust executes `fs_write_file` / shell commands trusting that the webview gate already ran. The policy lives in Rust but the enforcement lives in compromisable territory: a prompt-injected model, a bug in the gate wrapper, or a supply-chain compromise of any npm dependency in the webview can invoke mutating IPC directly, with no Rust-side re-check. The audit log likewise only sees what individual commands self-report.

**Audit ledger holes** (Rust): `pty_open/write/close` (interactive shell, the highest-risk surface) record nothing; `mcp_call_tool` records nothing; `tts_speak` makes an unaudited external network call with no timeout and unbounded text; `pi_store_record_transcript` accepts unvalidated, unbounded JSON (disk DoS, silent corruption on resume); session-history writes are non-atomic (truncation on crash).

**Dead weight still aboard:** the sidecar remains bundled (83 MB `pi-host/dist` + 91 MB `node/dist` of resources) and rebuilt on every `tauri build`, despite being a non-default fallback; the legacy `ai/` stack is untouched at 13,092 LOC; workflow carries dead code (`videoThumbnail.ts`, 168 LOC unused) and an unwired `webviewAgentExecution.ts`.

**Frontend debt unchanged from yesterday:** `App.tsx` still 934 LOC with 18 hooks and ref-map plumbing; `PiTranscript.tsx` (now 1,050+ LOC) still renders every event unvirtualized, which matters more now that pi is the flagship surface; `PiPanel` still drives 20+ reads through the custom `PiControllerProvider`; `chatStore` fan-out persists (moot once `ai/` folds).

**Workflow approval conflation:** approving a workflow node hardcodes `approved: true` into the Pi session policy (`nativeAgentExecution.ts:118-130`), so one click authorizes every tool the agent step may use, with no per-tool gating and no schema version migration for saved workflows (`WORKFLOW_DOCUMENT_VERSION = 1`, no migration code).

**Infra gates still open (re-verified today):** Biome linter disabled (`biome.json`), `pnpm audit` non-blocking (`ci.yml:29-30`), no bundle-size gate, no frontend coverage tracking, no e2e suite, zero tests in editor/git-history/theme.

---

## 3. The SOTA target architecture

One sentence per product-thesis exception, then the decisions that remove it:

1. *Terminal first*, except the app ships a 174 MB Node sidecar it no longer uses. Remove it (D2).
2. *Agent as a primitive*, except there are still two agent stacks (pi + legacy ai). Converge (D3).
3. *Trust by default*, except tool approval is enforced in the webview. Move enforcement into Rust (D1).
4. *Featherweight*, except dead fallbacks, dead code, and an unguarded TTS path ride along. Delete and harden (D2, D5).

### D1. Trust boundary doctrine: the webview proposes, Rust disposes

The single most important change. Options considered:

- **A. Status quo** (webview-only gate). Rejected: loses defense-in-depth precisely where the product promises it. The threat model for an agentic terminal includes prompt injection by construction; the enforcement point must sit below the layer the model can influence.
- **B. Move the whole agent loop into Rust.** Rejected: months of rewrite, loses the pi SDK, no incremental path.
- **C. Rust-side approval verification via grants.** Chosen. Mechanics:
  - When the user approves a tool call, the frontend calls a new `pi_approval_grant` command recording `(sessionId, toolCallId, toolName, argsHash)` in Rust state; denials are recorded too. This resurrects the sidecar-era `native_tool_approvals` machinery (`native_tools.rs:106-137`), which already exists and is tested.
  - Agent-initiated mutating calls (`fs_write_file`, `fs_create_*`, `fs_rename`, `fs_delete`, `shell_session_run`, `shell_bg_spawn`, `mcp_call_tool`) accept an optional `agentGrant: { sessionId, toolCallId }`. When present, Rust verifies the grant against policy (Auto passes, Ask requires a recorded grant, Deny blocks) and consumes it (single-use) before executing, then writes the audit entry.
  - Calls without a grant from the pi tool layer are rejected; direct user-driven UI calls (editor save, explorer rename) are unaffected because they do not originate from the agent bridge.
  - The webview gate stays as UX (it produces the approval card); it just stops being the security boundary.
  - The existing `check-pi-approval-boundary.mjs` string check is replaced by a behavioral test: deny a grant, assert no file written; approve, assert audit entry exists.

### D2. One runtime: delete the sidecar, the Node runtime, and the supervisor

The webview agent is at parity and default; the fallback now costs 174 MB of resources, sidecar rebuild time in every build, and a second code path in `pi-session-backend.ts`. Options: keep as fallback one more release (rejected: nothing routes to it and it will rot), or delete now. Chosen: delete now, in one commit series: `sidecars/`, `build-node-runtime.mjs`, `build-pi-host-bundle.mjs`, `smoke-pi-host-bundle.mjs`, tauri resources entries, `beforeBuildCommand` sidecar step, `pi/host.rs` + `host/bridge.rs` + `host/protocol.rs` supervisor (keep `native_tools.rs` for D1, `store.rs`, `state.rs`), the `sidecarBackend` branch, and the `USE_WEBVIEW_AGENT` flag itself. The git history preserves it if ever needed.

### D3. One agent UX: fold legacy `ai/` onto pi sessions

Unchanged from yesterday's plan in intent, now easier: provider/model config is already unified, and the pi runtime is proven. Migrate the docked composer, mini window, and status-bar controls to pi sessions; provide one-time import of `terax-ai-sessions.json`; then delete `ai/lib/agent.ts`, `ai/tools/`, the chatStore agent loop, and the AI-SDK chat transport (~10k of the 13k LOC). The AI SDK provider packages stay (they power `ai_http_*` model calls through `ai/config.ts`). Slice order: composer, mini window, sessions list, bridges.

### D4. Workflow: two-level approval and a stable schema

- Split node-approval ("run this agent step") from tool-approval ("may this step write files"). The Pi session created for a workflow step gets a policy snapshot, not `approved: true`; Ask-level tools either pause the run for an inbox approval or require the user to pre-grant named tools on the node ("this step may: write_file, bash_run"), which becomes part of the saved document and is shown at run time. Pre-granting is the default UX; it keeps unattended runs possible while making authority explicit and auditable. Wire `webviewAgentExecution.ts` (currently dead) as the execution path so workflow agents run on the same runtime as chat (one runtime everywhere, including D1 grants).
- Add `migrateWorkflowDocument` with a version enum before any further schema change; round-trip tests.
- Delete `videoThumbnail.ts`; split `WorkflowCanvas.tsx` (1,053 LOC) into memoized node/edge/panel children.

### D5. Voice as a first-class capability, or not at all

The uncommitted TTS work is product-appropriate (read agent replies aloud) but currently violates the boundary rules. Before commit: route the Cartesia call through the audited net layer (timeout, response-size cap, capability audit entry `app.tts`), cap input text (~4 KB), keep the macOS-native path as default when available, and decide the feature gate honestly (it currently hides behind `openclicky`, which is unrelated; give it its own `voice` feature or none). Streaming TTS and STT can come later; the boundary comes first.

### D6. Frontend architecture (carried forward, re-prioritized)

1. **Virtualize `PiTranscript`** with the existing `@tanstack/react-virtual` dependency, preserving stick-to-bottom. Now the top frontend item: pi is the flagship surface and sessions are long.
2. **Replace `PiControllerProvider` with a zustand store** with narrow selectors; split `PiPanel` (1,008 LOC) into list/transcript/composer children.
3. **Finish the App.tsx split** the shell refactor started: surface contexts (code, agent, layout) instead of 17 ref maps and 18 callbacks; collapse the 18 `useApp*` hooks into 4 coordinators; keep the two documented invariants (unconditional `AiComposerProvider` mount, never-unmount tabs) locked by a regression test. Target under 400 LOC.
4. chatStore selector work becomes mostly moot via D3; apply narrow selectors to whatever survives.

### D7. Backend hardening checklist

- PTY commands wrapped in capability audit (policy Auto, but recorded): the ledger must be complete to be a ledger.
- `mcp_call_tool` audited with tool name and server id.
- `pi_store_record_transcript`: parse-validate JSON, cap size (10 MB), reject otherwise; make session-history writes atomic (same temp-rename pattern as transcripts).
- Expand the secret deny-list (`~/.aws/credentials` file, `~/.docker/config.json`, `~/.config/gcloud/`, `~/.kube/config`).
- Remove remaining vestiges (`sync.rs`, `proc.rs` if unused) and the `#![allow(dead_code)]` HTTP MCP file-level escape.

### D8. Quality gates (unchanged from yesterday, still all open)

Enable Biome linter in CI; make `pnpm audit` blocking for high/critical; bundle-size budget gate after `vite build`; frontend coverage with floors; e2e golden flows via WebdriverIO + tauri-driver on Linux (terminal cwd, file edit/save, git stage/commit, pi prompt + approval round-trip with a mocked provider); tests for editor/git-history/theme; shellcheck on PTY init scripts; component-size warning at 600 LOC.

---

## 4. Execution plan

Ordering principle: security boundary first (it gates everything the agent does), deletion second (it removes the code that would otherwise need maintaining through the rest), convergence third, polish last.

### Progress log

**2026-06-11 - Phase A core landed (A1, A2, A3, partial A4).** All tests green: 368 Rust lib tests + integration suites, 940 frontend tests, tsc, clippy (all targets), approval-boundary check.

- **A1 (D1) Rust-side approval enforcement.** New `src-tauri/src/modules/pi/agent_tools.rs`: `PiAgentToolState` (single-use grant registry + audit ledger) and commands `pi_agent_tool_execute`, `pi_approval_grant`, `pi_agent_session_forget`, `pi_agent_tool_audit`. The webview agent now routes every native and MCP tool call through `pi_agent_tool_execute`, which authorizes the cwd against the live `WorkspaceRegistry`, evaluates capability policy (Auto passes; Ask requires a recorded grant; Deny blocks), consumes the grant single-use, runs the tool through the existing `execute_with_context` dispatcher (workspace-scoped + secret-deny-list), and audits the outcome. Frontend: `pi-tools.ts` gained `executeAgentTool`/`grantAgentTool`; `pi-session.ts` routes the seven native tools + MCP through the executor and records a grant on approval; `sessionId` is threaded through `createTauriAgent`. The webview approval card is now UX only - Rust is the boundary. Verified safe: the native dispatcher accepts absolute paths inside the workspace (`fs/safety.rs` `candidate_path`), so routing did not change agent path semantics.
- **A2 Behavioral boundary tests.** Rust unit tests in `agent_tools.rs` prove filesystem-level enforcement (denied write lands nothing, approved write lands once, grant is single-use, cross-tool grant rejected, cwd-escape blocked). Frontend `pi-session.boundary.test.ts` proves a denied gate never reaches the executor and an approved gate records a grant before executing; `pi-tools.test.ts` locks the IPC envelope and native-name mapping. `check-pi-approval-boundary.mjs` extended to guard the live webview path.
- **A3 Backend hardening.** `pi_store_record_transcript` now validates JSON and caps size at 10 MB; session-history `save_to_path` is now atomic (temp + rename), matching transcript writes. `pty_open` and `mcp_call_tool` now record to the capability audit (`app.pty_session`, `app.mcp_tool` added to the manifest) so the ledger covers every OS-touching surface. Secret deny-list extended with `.docker`, `.kube`, `gcloud`, `kubeconfig`.
- **A4 (D5) partial.** Voice/TTS hardened: 30s request timeout, 4096-char input cap, and the module compiles in the default build (rodio/objc2 paths correctly gated behind the `openclicky` feature). Remaining: route the Cartesia call through the audited net layer and give voice its own feature flag.

**Next:** A5 (CI gates: enable Biome linter, make `pnpm audit` blocking, add bundle-size budget), then Phase B (delete the sidecar).

**2026-06-11 (later) - build unblock + A5 partial.** A separate stream of uncommitted WIP (Rust `voice` TTS/STT/wake-word/PTT and an `agents` module) had broken the default `cargo build`: the modules used `cpal`, `tokio-tungstenite`, and `chrono` unconditionally while those deps are gated behind the `openclicky` feature, and a `global-shortcut:default` capability was added to `capabilities/default.json` for a plugin only present under `openclicky`. Fixes: gated `pub mod voice;` / `pub mod agents;` behind `openclicky`, moved their `.manage()` calls into a `#[cfg(feature = "openclicky")]` block (matching the `workflow` pattern), gated the conditional imports, and removed the stray capability line. Default build is green again (368 Rust lib tests + integration, clippy clean) and the openclicky internals are preserved for that feature's owner to finish.

- **A5 partial.** Bundle-size gate delivered: `scripts/check-bundle-size.mjs` + `pnpm check:bundle-size` + a CI step after `pnpm build`. Sums gzipped JS against a 2.05 MB budget (current 1.79 MB). **Deferred:** making `pnpm audit` blocking (two transitive `fast-uri` highs are unfixable without a `pnpm.overrides` + lockfile bump, which is unsafe while `package.json` is in active flux), and enabling the Biome linter (currently `enabled: false`; turning it on would flag across the whole actively-edited codebase). Both should land on a clean tree.
- **Phase B deferred** to a clean tree. The sidecar deletion is coupled to rewiring workflow execution off the native (sidecar-spawning) `pi_session_create` path onto the webview agent - really Phase C work - and the working tree currently carries large unrelated WIP plus failing sidebar tests. The target files (`sidecars/`, `pi/host*.rs`, build scripts, workflow) are not themselves in that WIP, so the deletion is safe to do once the tree is committed.

**Known TTS bug - FIXED 2026-06-11:** `voice/tts/mod.rs` `drain_queue` now uses `tauri::async_runtime::spawn` instead of `tokio::spawn` (the `3d4fcff` panic pattern); the `tts_status` queued count now reports `queue.len()`.

**2026-06-11 (later still) - A5 completed.** Both deferred A5 gates are now done; the bundle gate from earlier remains.

- **Audit blocking.** The two transitive `fast-uri` highs (path traversal, host confusion) are fixed by `fast-uri >= 3.1.2`. Added a `pnpm-workspace.yaml` override (`fast-uri@<3.1.2: ">=3.1.2"`; pnpm 11 moved overrides out of `package.json`), refreshed the lockfile to `fast-uri@3.1.2`, and made the CI audit step blocking (removed `continue-on-error`). `pnpm audit --prod --audit-level high` now exits clean.
- **Biome linter enabled.** `biome.json` linter turned on (`recommended: true`) with a tuned rule set, plus a `lint` CI step and `pnpm lint`/`lint:fix` scripts. Rules tuned for this codebase: `noControlCharactersInRegex` off (a terminal emulator parses ANSI/control chars in regexes), `noNonNullAssertion` off (established style; TS strict covers safety), and `useExhaustiveDependencies`, `noArrayIndexKey`, `useHookAtTopLevel`, `useOptionalChain`, `noDescendingSpecificity` plus the `a11y/*` set downgraded to `warn` for incremental cleanup (104 warnings remain, surfaced in CI logs but non-blocking). Generated `src/components/ui/**` and `src/components/ai-elements/**` are excluded from linting (regenerated via shadcn, not hand-edited). Genuine error-level issues were fixed by hand: `useIterableCallbackReturn` (forEach callbacks with implicit returns → block bodies, 7 sites), `noAssignInExpressions` (regex/index-of while-loops rewritten), `noImplicitAnyLet`, `noAccumulatingSpread` (O(n²) reduce-spread → mutate), `noBannedTypes` (`{}` → `Record<string, never>`), and one justified `noExplicitAny` biome-ignore. `tsconfig` lib bumped ES2020 → ES2022 (matches the Vite build target; fixes `Object.hasOwn`).
- **Lesson recorded:** an initial `biome lint --write --unsafe` run was reverted. Its `useExhaustiveDependencies` autofix silently rewrote ~27 files' hook dependency arrays (e.g. dropping `[sessionId]`/`[cacheKey]`/theme deps to `[]`, breaking effect reactivity - including the terminal theme-reapply effect) and overrode intentional `eslint-disable` omissions. Those 27 files were restored to HEAD; only the safe, reviewed fixes above were kept. Do not run `--unsafe` on this codebase's hooks.

Verification (default build): `biome lint` exit 0, `tsc --noEmit` clean, `pnpm audit --audit-level high` clean, approval-boundary + bundle-size checks pass.

**2026-06-11 (final) - flagged loose ends resolved at the root.**

- **Sidebar tests (3 failing) fixed at the source of truth.** The failures were stale assertions: the registry (`src/modules/sidebar/registry.ts`, the single source of truth that `views.ts` re-exports) deliberately gained three new primary rail views (`automation`, `agent-manager`, `skill-browser`) for the agents/skills/automation features under construction. Updated `registry.test.ts` and `views.test.ts` to assert the intended registry (primary now Files/Git/Automation/Agents/Skills; full view list of 9; "no legacy Pi rail item" invariant retained) and made the icon-metadata test iterate the whole registry so new views are covered automatically.
- **TTS spawn fixed** (above).
- **Formatting enforced.** Ran `biome format --write ./src` (85 files normalized) and added a `Format check` CI step (`pnpm format:check`) so drift can't recur - the root cause was that CI never enforced it.

Final verification: 940/940 frontend tests pass, `tsc` clean, `biome lint` + `format:check` pass, 368 Rust lib tests + integration pass, clippy clean, approval-boundary + bundle-size pass, audit clean.

**2026-06-11 (Phase B core landed).** The Node sidecar no longer ships.
- Workflow agent + browser-automation execution rewired off the sidecar-spawning native path onto the webview Pi agent (`workflowPiWebviewApi`). Added an `autoApproveTools` mode to `webviewSessionCreate` so unattended workflow runs pre-approve their tools (matching the old native `policy: {approved:true}`) while Rust still records and audits every grant.
- Sidecar removed from the app and build: dropped the pi-host/node bundle from `tauri.conf` resources, removed `build:node-runtime`/`build-pi-host-bundle`/`smoke-pi-host-bundle` (build:sidecars now only builds the speech recognizer), deleted `sidecars/pi-host` + `sidecars/node` (~174 MB of bundled node_modules out of the installer) and the empty `sidecars/*` workspace entry, removed the Pi-host-smoke CI step, and rewrote `check-pi-approval-boundary` to guard only the webview path. Deleted the sidecar integration tests (`pi_state.rs`, `pi_host_protocol.rs`).
- **Deferred (Phase B tail):** the Rust host supervisor (`host.rs`/`host/bridge.rs`/`host/protocol.rs`), the sidecar-backed `pi_session_*` host commands, and the frontend `sidecarBackend` + `USE_WEBVIEW_AGENT` flag + `piNative.session*` wrappers remain as dead code (compile, unit-tested via mocks, never spawned). Their removal is a focused follow-up - it touches the command surface and the Pi diagnostics panel, so it wants runtime verification.
Verified: tsc, biome lint + format, 844 frontend tests, full `cargo test`, `clippy --all-targets`, approval-boundary, bundle-size all pass.

**2026-06-11 (Phase B complete).** The sidecar is fully gone, code and all.
- Workflow runs on the webview agent (autoApproveTools mode). Sidecar no longer bundled/built/shipped; `sidecars/` source + build scripts + smoke + sidecar integration tests deleted; boundary script rewritten webview-only.
- Deleted the dead Rust host supervisor (`host.rs` + `host/` + `state/compat.rs` + `state/requests.rs` + `provider_config`), ~5,500 lines; `PiState` is now a minimal store-backed struct. Removed the host-backed `pi_session_*` commands, keeping `pi_status`/`pi_diagnostics`/`pi_start`/`pi_stop` as host-free stubs so the diagnostics panel degrades gracefully. Survivors: archive/restore/fork/rollback, usage_summary, sessions_history, store_*, agent_tool_*, local_agents. Frontend `sidecarBackend` + `USE_WEBVIEW_AGENT` resolver branch removed; the webview backend is the only path.
- Minor cosmetic leftovers (the `USE_WEBVIEW_AGENT` constant, dead `piNative.session*` wrappers, `native.test.ts` session tests) remain but are harmless (compile, tests pass, never invoked).
Verified: full `cargo test` + `clippy --all-targets`, tsc, 844 frontend tests, lint, format, approval-boundary all pass.

**2026-06-11 (Phase D started).** Memoized `PiTranscript`'s `TranscriptItem` (it was unmemoized, unlike AiChat's already-memoized `RenderedMessage`), so a streaming token no longer re-renders all ~160 rows. Strict, safe improvement. **Deferred** the deeper Phase D items because they depend on runtime behavior that needs the app running to verify safely: true list virtualization (both transcripts already cap rendered rows at 160/80 via the ai-elements `Conversation`, which wraps `use-stick-to-bottom`; swapping in `@tanstack/react-virtual` is a scroll-behavior-sensitive integration with `StickToBottom`), the `PiControllerProvider`→zustand migration, and the `App.tsx` split (whose load-bearing invariants - unconditional `AiComposerProvider` mount, never-unmount tabs - are runtime-verified). Recommend doing these with the app open.

**2026-06-11 (Phase C/D re-assessment against the code).** After reading the actual implementations:
- **`PiControllerProvider`→zustand is NOT worth doing.** The earlier audit assumed it caused wide re-renders. It does not: each `usePiControllerState(key, initial)` is an independent `useState`; the ref-held store is only a *persistence* layer (it retains values across panel remounts), not a shared-subscription store. Updating one key already re-renders only that consumer. Migrating to zustand-with-selectors would give the same isolation - a lateral move with real regression risk across 5 files / 20+ call sites and zero perf gain. Dropped from the plan.
- **Full list virtualization stays deferred.** Both `PiTranscript` (160) and `AiChat` (80) already cap rendered rows via the ai-elements `Conversation`/`StickToBottom`, so the DOM is already bounded; the remaining gain (scroll further back + fewer nodes in the window) requires making `@tanstack/react-virtual` and `StickToBottom` share one scroll container, a scroll/anchor-sensitive change that needs the running app to verify. The `TranscriptItem` memoization (done) captured the safe, high-value part (no more all-rows re-render per streaming token).
- **`App.tsx` split stays deferred.** The `useApp*` coordinators already exist; App.tsx's bulk is *threading* their outputs between each other (e.g. `useCodeSurfaceManager` consumes ~7 outputs of `useSidebarManager` plus `useTabs`), so a safe extraction is a careful data-flow re-ordering whose correctness is runtime-verified (tab/PTY lifecycle, the unconditional `AiComposerProvider` mount). Not a blind change.
- **Phase C `ai/`→pi fold** is a multi-session migration (~10k LOC of the legacy chat stack: composer, mini window, sessions, transport) and cannot be done safely blind. **Workflow per-tool approval (D4)** is substantially addressed: the workflow rewire's `autoApproveTools` mode is the audited pre-grant model the plan called for (every grant still recorded by Rust); finer-grained per-node tool allow-lists are a follow-on feature.

**2026-06-12 (Phase E: e2e harness landed).** Added a WebdriverIO + `tauri-driver` harness that drives the packaged binary through the native WebView, unblocking the runtime-verified items above (virtualization, `App.tsx` split, the `ai/`→pi fold) with a regression net instead of manual testing. Pieces: `wdio.conf.mjs` (spawns `tauri-driver`, targets `src-tauri/target/release/terax`, asserts the binary exists in `onPrepare`); three golden-flow specs under `e2e/specs/` that need no AI provider, secrets, or network - `smoke` (boot, React root renders, title `Terax`, tab bar + terminal pane mount), `tabs` (open terminal tab from the new-tab menu raises the tab count, close lowers it), and `terminal` (a PTY-backed xterm mounts, its helper textarea takes keystrokes, the UI stays responsive). Five stable `data-testid` hooks were added to source (`tab-bar`, `new-tab-button`, `new-tab-terminal`, `terminal-pane`, `cwd-breadcrumb`) so specs do not depend on Radix internals or class names. A Linux CI job (`e2e`) installs `tauri-driver` + `webkit2gtk-driver`, builds the binary, and runs the specs under `xvfb`. **Platform note:** `tauri-driver` has no macOS backend (no WKWebView WebDriver), so the harness runs on Linux/Windows CI only; on the macOS dev box `pnpm e2e` loads the config and discovers all three specs but stops at `spawn tauri-driver ENOENT`, which is expected and documented in `e2e/README.md`. xterm draws to a WebGL canvas, so terminal text is unreadable via the DOM; the terminal spec asserts the input path structurally rather than scraping output. Validated locally: `tsc` clean, `pnpm lint` exit 0 (baseline warnings only), `format:check` clean, frozen-lockfile install clean, the `TabBar` test still green. Follow-up commit added two more flows - `command-palette` (Ctrl+Shift+P opens cmdk, types, Escape closes) and `new-editor` (Ctrl+E opens the new-file dialog, Escape cancels without writing) - both leaning on the window capture-phase shortcut path so they fire with the terminal focused; the README also lists the deferred flows (theme = separate window, git-history = needs a fixture repo, breadcrumb = needs a pinned shell for OSC 7).

**2026-06-12 (Phase D: App.tsx split + transcript virtualization).** Two of the deferred frontend items landed now that the e2e net exists.
- **App.tsx split.** Pulled three self-contained JSX clusters out of `App.tsx` (934 → 888 lines) into named presentational components: `AppBridges` (notification/event bridges + toast portal, composer-gated pair included), `AppOverlays` (command palette, shortcuts, new-file, updater, close dialogs), and `AppComposerDock` (the workspace surface + docked AI input bar, preserving the never-unmount/`inert` collapse). Pure render split - the coordinator hook sequence (which must stay in the component body, and whose ordering is load-bearing) is untouched, as are the `AiComposerProvider`/`PiControllerProvider` trees. The remaining ~470 lines are inherent: they are the coordinator wiring that threads each `useApp*` output into the next, which cannot be extracted without breaking hook order.
- **Transcript virtualization - chose native `content-visibility` over `@tanstack/react-virtual`.** `react-virtual` would have to co-own the scroll container with `use-stick-to-bottom`'s spring-based auto-follow; the two fight over `scrollTop` during streaming, which is exactly the anchor risk this item was deferred for and cannot be tuned without interactive testing. Instead, a shared `LazyRow` (`src/components/lazy-row.tsx`) wraps each non-streaming row in `content-visibility: auto` + `contain-intrinsic-size: auto 64px`, so off-screen rows skip layout/paint but stay in the DOM - the scroll height stays correct and StickToBottom is undisturbed. This let me **remove the hard 160/80 render caps** in `PiTranscript` and `AiChat` (which silently dropped older messages); the full history is now reachable, and unsupported WebViews simply ignore the properties and render normally. The streaming row stays `eager` (no containment, since it is on-screen and changes per token). The old "windows large transcripts" test now asserts the full history renders with `content-visibility:auto`. 844 frontend tests green, build clean, bundle 1786.7 KB (budget 2050.8 KB).

**2026-06-12 (Phase C: convergence design + first down-payment).** Mapped the real dependency graph before touching anything and it reshaped the work: `ai/` (~13.5k LOC) is consumed by ~55 files and splits into three layers - **shared infra** (keyring, model discovery, transport, security, the `AiComposerProvider`) that pi and settings already depend on; a **shared agent runtime** (`chatStore`, `tools/*`, `agents/*`) that workflow, model-compare, source-control, and agents all use; and only then the **chat surface** (`AiChat`/`AiInputBar`/`AiMiniWindow`/plan+todo) that genuinely duplicates pi's own surface (`PiComposer`/`PiTranscript`/`PiFloatingWindow`). A blind "delete `ai/`" would break settings, workflow, model-compare, source-control, git-history, editor autocomplete, and agents - and the converged surface cannot be regression-tested yet because the chat path needs a live BYOK provider (keys + network), which the e2e harness deliberately excludes. So Phase C is a staged sequence behind a stable public surface, written up in **`docs/phase-c-convergence-plan.md`**: Stage 0 stands up a deterministic mock provider + e2e (the precondition for changing the surface), Stage 1 unifies the run-status contract underneath the shared consumers, Stage 2 lets the pi runtime back the docked quick-ask composer behind a flag, Stage 3 retires the duplicate surface pieces (mini window, plan-diff, todo strip, transcript) once pi covers them, and Stage 4 collapses/renames the runtime. **First down-payment already landed:** the shared `LazyRow` (above) is exactly the convergence pattern - extract the genuinely-shared primitive, point both surfaces at it, retire the legacy side once pi covers the affordance. The deeper stages are intentionally not forced here: each needs the Stage 0 verification scaffold first, and landing a 10k-line surface migration blind into five+ dependent features without a test path would regress a core UX.

**2026-06-12 (Phase C Stage 0 landed).** Built the verification scaffold: a deterministic mock provider behind the `terax.e2e` localStorage flag (unset in normal use). `mockProvider.ts` streams a canned reply via a lazily-imported `MockLanguageModelV3` (keeps `ai/test` out of the main chunk); `mockFlags.ts` holds the statically-imported predicates; a hidden keyless `mock-echo` model is registered, filtered from the pickers, made the default selection under the flag, and the composer's has-a-provider gate accepts the flag; `buildConfiguredLanguageModel` short-circuits to the mock before any key check. `ai-chat.e2e.mjs` drives composer -> transport -> store -> transcript end to end. Adds `@ai-sdk/provider` as a type-only devDependency. tsc/lint/format clean, 844 tests green, bundle 1788 KB.

**2026-06-12 (Phase C Stage 1 landed).** Unified run-status contract. `src/modules/ai/lib/agentRun.ts` owns a normalized `AgentRunPhase` (`idle | preparing | streaming | awaiting-approval | error`) plus mappers from both runtimes (`chatStatusToPhase`, `piStatusToPhase`, `chatMetaToAgentRun`, `piSessionToAgentRun`) and `isAgentBusy`; status types are imported type-only so it stays a dependency-light leaf. **Scope was smaller than the plan assumed:** mapping the code showed only **source-control** reads run *status* from `chatStore` (`agentMeta.status`, for its `aiBusy` flag); workflow/model-compare/agents import `chatStore` for shared *infra* (`apiKeys`, `selectedModelId`, `getOrCreateChat`), not run status, so they did not change. Source-control now derives `aiBusy` from `isAgentBusy(chatStatusToPhase(...))` - behavior-identical today, runtime-agnostic for Stage 2. The pi producer is tested and ready. 6 mapper tests; 850 tests green, build clean.

**2026-06-12 (Phase C Stage 2: composer runtime seam).** Decoupled the docked quick-ask composer from the chat runtime, the load-bearing architectural half of Stage 2. `composerRuntime.ts` defines a `ComposerRuntime` (`sessionId`/`send`/`stop`); `composer.tsx` now calls `runtime.send(parts)`/`runtime.stop()` instead of `getOrCreateChat(sessionId).sendMessage(...)`, with the chat-specific side effects (`patchAgentMeta`, `openMini`) moved into the default `useChatComposerRuntime`. Behavior is byte-for-byte unchanged (850 tests + the `ai-chat` e2e cover it), and `useComposerRuntime()` is the one place a pi runtime gets selected by flag. **The pi implementation + flag flip is deliberately not forced:** it needs a pi-runtime mock (pi-ai's `Model` protocol differs from the AI-SDK `MockLanguageModelV3`, so it is a separate larger mock), interactive verification of the response-rendering/focus wiring, and a UX call on `isBusy` semantics (the composer excludes `awaiting-approval`, the contract includes it) - none safely doable blind. Seam committed; pi impl is a contained follow-up. Details in `docs/phase-c-convergence-plan.md`.

**2026-07-01 (hardening + coverage + Stage 3 prep).** A batch off a deep re-review (workflow: 8 subsystem analysts + a gate verifier). The verifier caught lint+format RED from uncommitted parallel WIP (TTS/PTT/3D feature work); fixed the 3 `useIterableCallbackReturn` errors in `PiPanel.tsx` and the format issue (left in the WIP, not committed). Then, all committed separately so the WIP stays the author's:
- **Updater key.** `tauri.conf.json` had a rotated minisign updater pubkey; documented the cutover + the existing-install hazard (old builds embed the old pubkey and reject new-key releases) in `docs/updater-key-rotation.md`.
- **C1 audit hardening.** Bounded the capability-audit entry `message` centrally at the ledger write choke point (4 KB, UTF-8-safe truncation) so a large MCP/native result can't bloat the in-memory ledger; MCP per-part text + item count were already capped in `sanitize.rs`. Deliberately did NOT add per-write PTY auditing (per-keystroke noise), TTS/transcribe auditing (direct UI commands, not agent-reachable), or `deny_unknown_fields` (the ignore-unknown path is already security-tested not to authorize). 3 tests.
- **C2 coverage.** Authored 141 unit tests for the three zero-test modules (editor `prompt`/`diffCache`, git-history `graph`/`remoteWebUrl`, theme `validateTheme`/`themeFiles`/`applyTheme`/registry; no production source touched), added `@vitest/coverage-v8` + a `test:coverage` script + a CI gate. The floor is per-file ratchets on the now-100%-covered pure logic rather than a fragile global percentage.
- **C3 pi-runtime mock.** `pi-mock.ts` wraps pi-ai's official faux provider so pi sessions run offline under the `terax.e2e` flag, unblocking e2e for the Stage 2 pi path and Stage 3. Verified locally at the pi-ai event-protocol level.
All green: 994 frontend tests, 427 Rust tests, clippy clean, build clean, bundle 1944 KB (budget 2050).

### Phase A: Boundary and hardening (week 1-2)

- A1. Implement D1 grants: `pi_approval_grant` command, grant verification in the seven mutating commands, single-use consumption, audit entries. Reuse `native_tools.rs` approval state. Rust tests: deny blocks, ask-without-grant blocks, auto passes, grant is single-use.
- A2. Behavioral approval test replacing the string-matching boundary script.
- A3. D7 items: PTY + MCP audit, transcript validation and size cap, atomic history writes, deny-list expansion.
- A4. D5 voice hardening before the TTS work is committed.
- A5. D8 cheap gates: Biome linter on, audit blocking, bundle budget.

Exit: every OS-touching agent action is verified and recorded in Rust; CI red on lint/audit/bundle regressions.

### Phase B: The great deletion (week 2-3, after A1)

- B1. D2: sidecar, Node runtime, build scripts, supervisor, fallback path, flag. Expected wins: about 174 MB of bundled resources gone, sidecar build steps gone, one code path.
- B2. Workflow dead code (`videoThumbnail.ts`); root-dir strays (`--full-page`, screenshot PNGs); placeholder `AGENTS.md`; vestigial Rust modules.
- B3. Re-baseline the bundle budget after the deletion.

Exit: `sidecars/` does not exist; installer measurably smaller; CI green.

### Phase C: Convergence (week 3-6)

- C1. D3 fold of legacy `ai/` onto pi sessions, sliced (composer, mini window, sessions, bridges), with session import.
- C2. D4 workflow approval split and `webviewAgentExecution` wiring; schema migration scaffolding.

Exit: one agent stack, one approval model spanning chat and workflows, `ai/` reduced to config + provider plumbing.

### Phase D: Frontend performance and structure (week 4-8, parallel with C except the agent surface slice)

- D6 items in order: PiTranscript virtualization, PiControllerProvider to store + PiPanel split, App.tsx coordinator completion, WorkflowCanvas split.

Exit: 60fps on a 500-event transcript; App.tsx under 400 LOC with invariant regression tests.

### Phase E: Quality depth (week 6-9, ongoing)

- D8 remainder: e2e suite, frontend coverage floors, tests for editor/git-history/theme, shellcheck.

---

## 5. Success metrics

- Exactly one agent runtime and one approval model; a tool call denied in the UI provably cannot execute (Rust test, not convention).
- The capability audit log accounts for every PTY, shell, fs-mutation, MCP, network, and TTS operation an agent triggers.
- `sidecars/` deleted; installer smaller by the bundled Node runtime and pi-host; build has no sidecar steps.
- Legacy `ai/` agent loop deleted; chat and workflow both run on pi sessions.
- CI blocks on lint, high-severity audit, bundle budget, frontend coverage floor, behavioral approval test, e2e golden flows.
- App.tsx under 400 LOC; 500-event transcripts scroll at 60fps; no component over 600 LOC without a recorded exception.

## 6. Non-goals

- No return to the sidecar or to `createAgentSession`; the webview-native pivot is accepted and built upon.
- No framework migrations (zustand stays; no XState, router, or jotai).
- No new heavy dependencies; virtualization uses `@tanstack/react-virtual` already present.
- No telemetry, per PRODUCT.md.
- Model-compare, inbox, and artifacts stay, but as supporting features behind lazy imports; they do not gate any phase above.
